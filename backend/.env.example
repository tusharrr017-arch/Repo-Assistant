# Copy to .env and fill in. Do not commit .env or API keys.

# Required: API key (OpenAI or OpenRouter)
OPENAI_API_KEY=sk-your-key-here

# Optional: use OpenRouter instead of OpenAI (set base URL + OpenRouter key above)
# OPENAI_BASE_URL=https://openrouter.ai/api/v1

# Optional: override model names
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small  (or openai/text-embedding-3-small for OpenRouter)
# OPENAI_CHAT_MODEL=gpt-4o-mini                   (or openai/gpt-4o-mini for OpenRouter)
# CHROMA_PERSIST_DIR=./chroma_data
# CHROMA_COLLECTION_NAME=codebase_qa
